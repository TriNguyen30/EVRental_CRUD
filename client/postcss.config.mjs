const config = {
  plugins: ["@tailwindcss/postcss"],
  autoprefixer: {},
  daisyui: {
    themes: ["light", "dark", "cupcake", "bumblebee", "corporate", "synthwave"],
  },
};

export default config;
